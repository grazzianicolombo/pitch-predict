# Dependências
node_modules/
**/node_modules/

# Variáveis de ambiente — NUNCA subir para o repo
# Todos os segredos são gerenciados pelo Doppler
.env
.env.local
.env.production
.env.staging
.env.*.local
**/.env
**/.env.*
!**/.env.example    # .env.example pode ser commitado (não tem valores reais)

# Doppler — tokens nunca devem estar em arquivo
.doppler-token
doppler-token.txt

# Build
frontend/dist/
frontend/build/

# Logs
*.log
npm-debug.log*
/tmp/

# Jobs persistidos localmente
backend/src/lib/jobs-store.json

# Uploads e dados temporários
uploads/
data/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
