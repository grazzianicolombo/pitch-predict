# Em produção (Vercel): usar /api — Vercel proxeia para o Railway (cookies same-origin)
# Em desenvolvimento local: usar http://localhost:3001/api
# Em produção (Vercel): usar /api — Vercel proxeia para o Railway (cookies same-origin)
# Em desenvolvimento local: usar http://localhost:3001/api
# URL Railway atual: https://pitch-predict-production.up.railway.app/api
VITE_API_URL=/api
